# Lint/format
mypy
pyright
ruff

# Tests
psutil
pytest
pytest-cov
pytest-mock
tox

# Optional dependencies
qiskit>=1.0
qiskit-aer
rustworkx

# Optional dependency. Pinned due to version changes often being incompatible
pyzx==0.8.0
