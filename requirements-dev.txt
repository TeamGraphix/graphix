# Style
black==22.8.0

# Tests
pytest
parameterized
tox

# Optional dependencies
qiskit
qiskit-aer
rustworkx
# jax does not work with python 3.8 on windows
jax[cpu]; python_version > '3.8' or sys_platform != 'win32'
jax_dataclasses; python_version > '3.8' or sys_platform != 'win32'
